{
  "$schema": "https://turbo.build/schema.json",
  "ui": "tui",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "env": ["NODE_ENV", "X_MICROCMS_API_KEY", "X_MICROCMS_REVALIDATE_SECRET"],
      "outputs": [".next/**", "!.next/cache/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "lint": {
      "env": ["X_MICROCMS_API_KEY", "X_MICROCMS_REVALIDATE_SECRET"]
    },
    "lint-fix": {},
    "test": {},
    "type-check": {}
  }
}
